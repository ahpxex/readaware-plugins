{
  "name": "readaware-plugin-theme-schedule",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outfile main.js --format esm",
    "test": "bun test",
    "check": "tsc --noEmit -p ../../tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
